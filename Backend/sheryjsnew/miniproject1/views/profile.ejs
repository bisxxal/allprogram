<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>

    <style>
       textarea,body::-webkit-scrollbar{
        width: 0;
       }
    </style>
    
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900">
        <div class="flex justify-end pr-10"><a class="text-white px-5 py-1 mt-3 py- rounded-lg bg-red-600 text-medium" href="/logout">Logout</a></div>
        <div class="text-white px-10">
            <div class="flex gap-4 items-center ">
                <a href="/profile/upload">
                    <div herf="/profile/upload" class="h-14 w-14  overflow-hidden rounded-full "><img class="object-cover h-full w-full" src=" /images/uploads/<%= user.profilePic%>" alt=""></div>

                </a>
                <h1 class="mb-1 text-3xl">Hello, <span class="font-semibold capitalize text-green-400"> <%= user.name%> </span> 👋</h1>
            </div>
             <h1 class="text-[18px] mt-3">You can create a new post </h1>

             <form action="/post" method="post">
                <textarea class="block outline-none resize-none mt-6 bg-transparent border-2 border-zinc-700 rounded-md px-3 py-2 w-1/3" placeholder="What's in your mind ?" name="content" id=""></textarea>
                <input class="block px-3 py-1 bg-blue-500  mt-2 rounded-md font-semibold"  type="submit" value="Create Post">    
                 </form>

                 <div>
                     <h1 class="mt-10">Your posts..</h1>

                     <div class="postcontainers py-5 w-full ">

                    <% user.posts.reverse().forEach((post)=>{ %>
                        <div class=" post bg-zinc-800 border border-zinc-600 w-1/3 px-3 rounded-md mt-3 py-2">
                            <p class="text-blue-400 mb-1">@  <%= user.userName %></p>
                            <h2 class="text-sm w-full"><%= post.content %></h2>

                            <small> <%=post.likes.length %> likes</small>
                            <div class="flex gap-8 mt-3">
                                <a class="text-red-500  " href="/like/<%= post._id %>">
                                    <%= post.likes.indexOf(user._id) === -1 ? "like" :"Dislike" %>
                                </a>
                                <a class="text-zinc-500  " href="/edit/<%= post._id %>">Edit</a>
                            </div>
                        </div>

                    <% }) %>


                     </div>
                 </div>          
         
            </div>
    </div>
</body>
</html>